# Backend API base URL (FastAPI)
# Local backend typically runs on port 8000
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Optional: app display name (if you want to use it in layout/title later)
NEXT_PUBLIC_APP_NAME=Full Stack FastAPI (Next.js)